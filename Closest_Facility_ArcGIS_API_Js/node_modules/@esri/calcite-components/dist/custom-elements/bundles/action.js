import { CalciteAction, CalciteLoader, CalciteIcon } from "../index";
import { register } from "../utils";
register({
    "calcite-action": CalciteAction, "calcite-loader": CalciteLoader, "calcite-icon": CalciteIcon
});
